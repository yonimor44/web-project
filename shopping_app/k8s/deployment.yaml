apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopping-backend-deployment
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: shopping-backend
  template:
    metadata:
      labels:
        app: shopping-backend
    spec:
      containers:
      - name: shopping-backend
        image: localhost:5000/shopping-backend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        env:
          - name: JWT_SECRET
            value: "super-secret-jwt-key-that-is-very-long-12345"
          - name: DB_HOST
            value: "postgres-service"
          - name: DB_PORT
            value: "5432"
          - name: DB_USERNAME
            value: "postgres"
          - name: DB_PASSWORD
            value: "postgres"
          - name: DB_NAME
            value: "shopping_db"
          - name: SESSION_SECRET
            value: "super-secret-session-key"
          - name: GOOGLE_CLIENT_ID
            value: "dummy-google-client-id"
          - name: GOOGLE_CLIENT_SECRET
            value: "dummy-google-client-secret"
          - name: GOOGLE_CALLBACK_URL
            value: "http://localhost:3000/auth/google/callback"
          - name: FRONTEND_URL
            value: "http://localhost:30080"
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5