apiVersion: v1
kind: Service
metadata:
  name: shopping-backend-service
spec:
  selector:
    app: shopping-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP